
version: 1
recipe:
  name: "cyl_demo_helix"
  frame: "scene"
  tool: "spray_nozzle_01"
  substrate:
    file: "cylinder_d150_h150.stl"   # exakt wie in surfaces.yaml (filenames)
    side: "helix"                    # oder "top"
    parameters:
      speed_m_s: 0.15
      stand_off_mm: 15.0

  paths:
    - name: "cyl_helix"
      type: "helix"
      valid: true
      parameters:
        speed_m_s: 0.15   # override möglich
      generator:
        axis: z
        pitch_mm: 10.0
        start_angle_deg: 0.0
        direction: ccw
        outside_mm: 1.0
        margin_top_mm: 0.0
        margin_bottom_mm: 0.0